<?xml version="1.0" encoding="UTF-8"?>
<persistence version="2.2" xmlns="http://xmlns.jcp.org/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence
             http://xmlns.jcp.org/xml/ns/persistence/persistence_2_2.xsd">

    <!-- Unidad de Persistencia para E-commerce -->
    <persistence-unit name="Ecommerce" transaction-type="RESOURCE_LOCAL">

        <!-- Entidades JPA -->
        <class>entidades.Usuario</class>
        <class>entidades.Direccion</class>
        <class>entidades.Cliente</class>


        <properties>
            <!-- Configuración de conexión a MySQL -->
            <property name="javax.persistence.jdbc.url" value="jdbc:mysql://localhost:3306/Ecommerce?createDatabaseIfNotExist=true&amp;zeroDateTimeBehavior=CONVERT_TO_NULL"/>
            <property name="javax.persistence.jdbc.user" value="root"/>
            <property name="javax.persistence.jdbc.driver" value="com.mysql.cj.jdbc.Driver"/>
            <!--CONTRASEEÑAA -->
            <property name="javax.persistence.jdbc.password" value="murrietaBD2025%"/>

            <!-- Proveedor JPA (Hibernate) -->
            <property name="hibernate.dialect" value="org.hibernate.dialect.MySQL8Dialect"/>

            <!-- Estrategia de generación de esquema automática -->
            <!-- create: borra y recrea las tablas cada vez -->
            <!-- update: actualiza sin borrar datos -->
            <!-- validate: solo valida el esquema -->
            <property name="hibernate.hbm2ddl.auto" value="create"/>
            <property name="javax.persistence.schema-generation.database.action" value="create"/>

            <!-- Mostrar SQL en consola (útil para desarrollo) -->
            <property name="hibernate.show_sql" value="true"/>
            <property name="hibernate.format_sql" value="true"/>
            <property name="hibernate.use_sql_comments" value="true"/>
        </properties>
    </persistence-unit>
</persistence>
